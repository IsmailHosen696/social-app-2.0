/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com', "images.unsplash.com", "www.instagram.com"],
  },
}

module.exports = nextConfig
